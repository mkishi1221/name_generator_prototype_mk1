#!/bin/bash

# This is to make sure that the script works off absolute imports. 
# (How can I write the file location below so that it's set to whatever the current directory is? Hopefully it can then be universal)
export PYTHONPATH="${PYTHONPATH}:/Users/masayukikishi/Dropbox\ \(Personal\)/masayuki_kishi/repositories/create_names"